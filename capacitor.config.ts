import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stock.management',
  appName: 'Stock Mangement',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
